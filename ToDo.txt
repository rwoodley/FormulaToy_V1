- get dismiss button working on sharing screen
- save 'distance from origin' in params too
- make animated gif
- allow camera to spin different ways
- help pages
- some kind of help with errors.
- label axes




        console.log(x + ',' + y + ',' + z); \



Darin Allen Woodley born: 26 Jan 2001 in Bloomington, Monroe, Indiana

cone:
    var z = (x*x + y*y);  


    var z = (petalPeak*x*x + petalTrough*y*y);  
    temprad = radius*Math.cos(theta * nPetals/2 );
    x = temprad*Math.cos(theta+offsetRadians);
    y = temprad*Math.sin(theta+offsetRadians);




    if (kind == 9)
        return algoPetals(theta, radius, zdelta, 8);
    if (kind == 10)
        return algoPetals(theta, Math.exp(radius)/Math.exp(1), zdelta, 8);
